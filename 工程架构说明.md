# NPWT 负压伤口治疗仪 - 工程架构说明文档

**版本**: v1.0 俄语/英语版本  
**芯片**: PIC18F46J11  
**编译器**: HI-TECH C Compiler  
**生成时间**: 2025-10-19

---

## 📊 工程分层架构

```
┌─────────────────────────────────────────────────────────────┐
│                    第1层：主程序入口                          │
│  npwt_dis_main.c - 程序入口main()、主循环(20ms)、状态管理   │
└─────────────────────────────────────────────────────────────┘
                               ↓
┌─────────────────────────────────────────────────────────────┐
│                    第2层：功能控制层                          │
│  ┌─────────────────┬──────────────────┬──────────────────┐  │
│  │npwt_con_main.c  │npwt_con_over.c   │npwt_con_ofile_   │  │
│  │电池监测/故障判断│压力双位控制算法   │load_00.c         │  │
│  │                 │动态阈值计算       │负载控制/PWM占空比│  │
│  └─────────────────┴──────────────────┴──────────────────┘  │
│  ┌───────────────────────────────────────────────────────┐  │
│  │npwt_con_ifile_adc.c - ADC采集与数字滤波               │  │
│  └───────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────┘
                               ↓
┌─────────────────────────────────────────────────────────────┐
│                  第3层：显示与交互层                          │
│  ┌──────────────────────┬───────────────────────────────┐  │
│  │npwt_dis_ifile_key_   │npwt_dis_ofile_lcd_02.c        │  │
│  │00.c                  │LCD显示驱动                     │  │
│  │按键扫描/模式切换      │字符和图形绘制                  │  │
│  └──────────────────────┴───────────────────────────────┘  │
└─────────────────────────────────────────────────────────────┘
                               ↓
┌─────────────────────────────────────────────────────────────┐
│                   第4层：系统支撑层                           │
│  ┌──────────────────┬──────────────────┬────────────────┐  │
│  │npwt_dis_sys_ini_ │npwt_dis_sys_uart_│sys_cpu.c       │  │
│  │00.c              │00.c              │CPU配置         │  │
│  │系统初始化/中断ISR│UART通信/参数配置 │                │  │
│  └──────────────────┴──────────────────┴────────────────┘  │
└─────────────────────────────────────────────────────────────┘
                               ↓
┌─────────────────────────────────────────────────────────────┐
│                   第5层：硬件驱动层                           │
│  ┌─────────┬──────────┬──────────────────┬──────────────┐  │
│  │adc.c    │Flash.c   │BIOS_JLX1864G_   │ziku/*.c      │  │
│  │ADC驱动  │Flash驱动 │139.c            │字库数据      │  │
│  │         │          │LCD硬件驱动      │              │  │
│  └─────────┴──────────┴──────────────────┴──────────────┘  │
└─────────────────────────────────────────────────────────────┘
```

---

## 🔄 主要数据流

### 1. 压力控制流程
```
ADC采集(每5ms)
   ↓
压力计算 (ADC值 → mmHg)
   ↓
压力判断 (与目标压力比较)
   ├─ 压力过低 → 开启气泵 + 开启电磁阀VAL2
   ├─ 压力过高 → 关闭气泵 + 延时后关闭VAL2
   └─ 压力正常 → 堵塞检测 + 液位检测
```

### 2. 用户交互流程
```
按键扫描(每20ms)
   ↓
按键值判断
   ├─ 上/下键 → 调整参数
   ├─ OK键 → 确认/启动
   ├─ 静音键 → 切换静音模式
   └─ 长按组合 → 解锁/语言切换
   ↓
LCD显示更新
```

### 3. 故障检测流程
```
传感器采集
   ↓
故障判断
   ├─ 泄漏检测：补气时间过长（44秒）
   ├─ 液位检测：30秒内膨胀4次
   ├─ 堵塞检测：压力7次记录无变化
   └─ 空闲检测：5分钟无操作
   ↓
设置故障标志位 (flager_a)
   ↓
声音报警 + LCD显示错误
```

---

## 📂 文件功能详解

### 主程序文件

| 文件名 | 行数 | 功能说明 |
|--------|------|----------|
| `npwt_dis_main.c` | 720 | **主程序入口**：main()函数、主循环、故障报警、电池管理 |

### 控制层文件

| 文件名 | 行数 | 功能说明 |
|--------|------|----------|
| `npwt_con_main.c` | 207 | **控制主逻辑**：电池电量分级检测、故障优先级判断 |
| `npwt_con_over.c` | 395 | **压力综合控制**：双位控制算法、动态阈值、堵塞检测 |
| `npwt_con_ofile_load_00.c` | 186 | **负载控制**：压力计算、PWM查表、泄漏检测 |
| `npwt_con_ifile_adc.c` | 74 | **ADC采集控制**：多通道采集、数字滤波 |

### 显示与交互层文件

| 文件名 | 行数 | 功能说明 |
|--------|------|----------|
| `npwt_dis_ifile_key_00.c` | ~850 | **按键处理**：扫描、模式切换、长按检测、锁定/解锁 |
| `npwt_dis_ofile_lcd_02.c` | 1693 | **LCD显示**：字符绘制、界面布局、图标显示 |

### 系统支撑层文件

| 文件名 | 行数 | 功能说明 |
|--------|------|----------|
| `npwt_dis_sys_ini_00.c` | 240 | **系统初始化**：GPIO配置、定时器配置、中断服务ISR |
| `npwt_dis_sys_uart_00.c` | 53 | **UART通信**：参数配置、工厂测试接口 |
| `sys_cpu.c` | 13 | **CPU配置**：时钟配置相关 |

### 硬件驱动层文件

| 文件名 | 行数 | 功能说明 |
|--------|------|----------|
| `adc.c` | 78 | **ADC驱动**：3通道ADC采集（压力、电池、液位） |
| `Flash.c` | 72 | **Flash存储**：读写用户配置和校准参数 |
| `BIOS_JLX1864G_139.c` | 1818 | **LCD驱动**：JLX12864G液晶屏硬件驱动 |
| `ziku/8X16.c` | 398 | **英文字库**：8×16点阵ASCII字符 |
| `ziku/BIOS_ZK_DAW.c` | 1964 | **中文大字库**：16×16点阵汉字 |
| `ziku/BIOS_ZK_X1.c` | 705 | **中文小字库**：小号汉字 |
| `ziku/6X12.c` | 294 | **小字库**：6×12点阵（备用） |

---

## ⚙️ 核心算法说明

### 1. 压力双位控制算法（Bang-Bang Control）

**核心函数**: `STAT_conNewa()` in `npwt_con_over.c`

```
控制逻辑：
  if (当前压力 ≤ 目标压力 - con_lo_delta)
      → 开启气泵补气
  else if (当前压力 ≥ 目标压力 + con_hi_delta)
      → 关闭气泵
  else
      → 保持状态，进行堵塞/液位检测

动态阈值（自适应）：
  con_lo_delta = 目标压力 × 10%
  con_hi_delta = 目标压力 × 5~15%（根据压力范围）
```

**示例**：
- 目标120mmHg → 补气范围：108~126 mmHg
- 目标200mmHg → 补气范围：180~210 mmHg

### 2. 故障检测算法

#### 泄漏检测
```c
// 在 LEVEL_WarnA() 函数中
if (open_bum==FALSE)  // 不在补气状态
{
    if (cntcnt++ >= 200)  // 200×100ms = 20秒
    {
        flager_a &= ~ERRA_LQ;  // 清除泄漏标志
    }
}
else  // 正在补气
{
    if (load_perioda_up > 2200)  // 2200×20ms = 44秒
    {
        flager_a |= ERRA_LQ;  // 设置泄漏标志
    }
}
```

#### 液位检测
```c
// 在 LEVEL_WarnA() 函数中
if (cnt_cnta > 4)  // 膨胀次数 > 4次
{
    if (cnt_cntaa <= 8000)  // 在40秒内
    {
        flager_a |= ERRA_S;  // 液位满
    }
}
```

#### 堵塞检测
```c
// 记录最近8次压力值，如果完全相同则判定为堵塞
if (record_ds[7] == record_ds[0])
{
    flager_a |= ERRB_DS;  // 堵塞
}
```

### 3. PWM控制算法

**核心**: 定时器3中断，1ms周期

```c
// 占空比控制（在中断中）
if (pwm_cnt1 <= PWM_PERO)  // PWM_PERO=10 → 10ms周期 = 50Hz
    PUMP = 1;  // 气泵开启
else
    pwm_cnt1 = 0;  // 重置计数器

// 占空比查表（在PRESS_ConSubA()中）
if (目标压力 <= 20)      pwm_cnt2 = 4;  // 40%
else if (目标压力 < 100)  pwm_cnt2 = 4;  // 40%
else if (目标压力 <= 290) pwm_cnt2 = 5;  // 50%
else                      pwm_cnt2 = 6;  // 60%
```

---

## 🎯 关键全局变量

### 系统状态
| 变量名 | 类型 | 说明 |
|--------|------|------|
| `mod_main_a` | unsigned char | 当前工作模式 |
| `flager_a` | unsigned char | 故障标志位寄存器 |
| `err_codea` | unsigned char | 错误代码 |

### 压力控制
| 变量名 | 类型 | 说明 |
|--------|------|------|
| `mod_seta_preh` | unsigned short | 目标压力值（mmHg） |
| `adc_ps00` | unsigned short | 当前压力值（mmHg） |
| `con_hi_delta` | unsigned short | 上阈值增量 |
| `con_lo_delta` | unsigned short | 下阈值增量 |
| `valueK` | float | 压力校准系数（2.75） |

### 硬件控制
| 变量名 | 类型 | 说明 |
|--------|------|------|
| `PUMP` | bit | 气泵控制引脚 |
| `VAL1` | bit | 电磁阀1（排气阀） |
| `VAL2` | bit | 电磁阀2（放气阀） |
| `SPEAK` | bit | 扬声器/蜂鸣器 |

---

## 🕐 定时系统

### 定时器0（20ms主循环）
```c
中断频率: 50Hz (每20ms)
功能: 设置FLG_SYS_10MS标志，触发主循环任务

主循环任务（每20ms执行）：
  1. 按键扫描
  2. 模式切换
  3. LCD显示更新
  4. 故障检测
  5. 电池监控
  6. 排气控制
```

### 定时器3（1ms中断）
```c
中断频率: 1000Hz (每1ms)
功能: PWM波形生成 + 压力采集

中断任务：
  - PWM计数（50Hz气泵控制）
  - ADC采集（每5ms一次）
  - 压力控制算法（每5ms一次）
  - 系统时间累加
```

---

## 🔌 硬件接口

### GPIO端口分配

**PORTA（音频和ADC）**
- RA0: AN0（压力传感器，ADC输入）
- RA1: AUDIO_EN（音频使能）
- RA2: AUDIO_G2
- RA3: SPEAK（蜂鸣器输出）
- RA5: AUDIO_G0

**PORTB（按键和电磁阀）**
- RB0: VAL1（电磁阀1，排气阀）
- RB1: VAL2（电磁阀2，放气阀）
- RB2-7: 按键输入

**PORTC（电源和控制）**
- RC0: BAT_GOOD（电池电量良好）
- RC1: BAT_CHARGE（充电状态）
- RC2: POWER_ON（电源自锁）
- RC3: DRV_EN（屏幕背光驱动）
- RC4: GRE（绿色LED）
- RC5: YEL（黄色LED）
- RC7: PUMP（气泵控制）

**PORTD**: LCD数据线D0-D7

**PORTE**: LCD控制线

---

## 💾 Flash存储映射

**起始地址**: 0xa000

| 偏移 | 内容 | 说明 |
|------|------|------|
| +0 | dataREAD1 | 配置数据1 |
| +2 | dataREAD2 | 配置数据2 |
| +4 | dataREAD3 | 配置数据3 |
| +6 | dataREAD0 | 配置数据0 |
| +8 | dataK1 | 压力校准系数K1 |
| +10 | dataK2 | 压力校准系数K2 |
| +12 | dataK3 | 压力校准系数K3 |
| +14 | dataK4 | 压力校准系数K4 |
| +16 | key_silent_flag | 高字节：静音标志，低字节：语言选择 |
| +18 | mod_seta_prel | 上次设置的目标压力值 |

---

## 📋 工作模式说明

| 模式 | 代码 | 说明 |
|------|------|------|
| 系统初始化 | MOD_SYS (0) | 开机画面、自检、7秒后转入待机 |
| 等待指令 | MOD_WAT (1) | 待机状态，等待用户操作 |
| 连续模式 | MOD_LIX (2) | 持续维持负压 |
| 间歇模式 | MOD_JIX (3) | 高压-低压循环（时间可调） |
| 参数设定 | MOD_SET (4) | 设置目标压力、时间等参数 |
| 暂停模式 | MOD_ZHT (5) | 暂停治疗，保留设置 |
| 错误模式 | MOD_ERR (6) | 故障报警状态 |
| 关机模式 | MOD_OFF (7) | 关机 |
| 空闲超时 | MOD_TK (8) | 5分钟无操作保护 |
| 语言切换 | MOD_LANGUAGE (9) | 多语言选择界面 |

---

## 🚨 故障类型与优先级

| 优先级 | 错误码 | 标志位 | 说明 |
|--------|--------|--------|------|
| 1（最高） | ERR_DEV_IDLE | ERRB_TK | 设备空闲（5分钟） |
| 2 | ERR_JAMED | ERRB_DS | 堵塞 |
| 3 | ERR_CANISTER_REACHED | ERRA_YW | 液位警告 |
| 4 | ERR_AIR_LEAKAGE | ERRA_LQ | 空气泄漏 |
| 5 | ERR_CANISTER_FULL | ERRA_S | 液位满 |
| 6 | ERR_SENSOR_MALFUCTION | ERRA_S | 传感器故障 |
| 7 | ERR_PIPE_BLOCKED | ERRA_V | 管路堵塞 |
| 8（最低） | ERR_CANISTER_NOT_CON | ERRA_V | 未接收集罐 |

---

## 🔧 编译配置

### 编译器参数
```
-mcpu=18F46J11
-fno-short-double
-fno-short-float
-memi=wordwrite
-O3
-std=c99
-maddrqual=ignore
```

### 包含路径
```
-I"../../source/h"
-I"../../source/ziku"
```

---

## 📈 性能参数

| 参数 | 数值 |
|------|------|
| **主循环周期** | 20ms (50Hz) |
| **压力控制周期** | 100ms (10Hz) |
| **ADC采集周期** | 5ms (200Hz) |
| **PWM频率** | 50Hz（可调整为10kHz） |
| **压力范围** | 20-300 mmHg |
| **压力步进** | 10 mmHg |
| **压力精度** | ±5~15% |

---

## 🎯 关键技术特性

### 1. 自适应压力控制
- 动态阈值：根据目标压力自动调整死区
- 分段PWM：不同压力范围使用不同占空比
- 防超调：延时检测避免压力波动

### 2. 多重安全保护
- 泄漏检测：44秒补气超时
- 液位检测：30秒内膨胀4次
- 堵塞检测：7次压力记录无变化
- 空闲保护：5分钟自动锁定
- 低电保护：3.5V自动关机

### 3. 用户体验优化
- 静音模式：15秒自动取消
- 背光控制：30秒自动熄灭
- 按键锁定：防误操作
- 多语言支持：7种语言（当前2种）

---

## 📝 维护建议

### 代码规范
- ✅ 编码：UTF-8
- ✅ 缩进：TAB（宽度4）
- ✅ 行尾符：LF (Unix)
- ✅ 已添加详细中文注释

### 调试接口
- UART：波特率9600，用于参数配置
- 通过UART可以调整目标压力、校准系数等

### 版本升级路径
1. PID算法替代双位控制
2. 蓝牙/WiFi通信
3. 数据记录功能
4. 更多语言支持

---

**文档生成工具**: 自动代码分析  
**文档版本**: 1.0  
**最后更新**: 2025-10-19

